module.exports = closestEnemy = function (last_Arr, destin) {
    console.log("closestEnemy function fires");

    // getEnemyCount & locations:
    let enemyCounter = getEnemyCount(homePawnType);

    for(let x = 0; x < enemyCounter.length; x++) {
        
    }

    enemyMoves(last_Arr, destin);
};
